name: Website Build & Push

on:
  push:
    branches: [master]

jobs:
  build:
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v4
      - name: Run a script
        run: |
          npm run build
          cp dist/torrenteservidores/browser/* /var/www/
